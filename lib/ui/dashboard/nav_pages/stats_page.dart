import 'package:flutter/material.dart';

class StatsNavPage extends StatelessWidget {
  const StatsNavPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Stats'),
      ),
    );
  }
}
